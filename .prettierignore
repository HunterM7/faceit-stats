node_modules
dist
build
coverage
*.lock

# HTML: форматирование и перевод строки в конце — через ESLint (@html-eslint), не Prettier.
**/*.html

# SVG: не трогаем разметку (флаги, регионы, иконки).
**/*.svg
